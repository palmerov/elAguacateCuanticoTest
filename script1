import random

def generar_codigo(longitud=6):
    return ''.join(str(random.randint(0, 9)) for _ in range(longitud))

codigo = generar_codigo()
print(f"Código generado: {codigo}")
print("Codigo python mivida")